var Header = React.createClass({
    render: function () {
        return(
            <header>

            </header>
            )
    }
})
ReactDOM.render(<Header />, document.getElementById('react-content'))
